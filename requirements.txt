# === Core ML stack ===
# TensorFlow 2.16.x includes GPU support via pip on Linux/WSL2 (ensure NVIDIA driver installed on host)
tensorflow==2.16.*

# Keras Tuner for LR search (works with Keras 3 bundled in TF 2.16)
keras-tuner>=1.4.6,<1.5

# === Notebook environment ===
jupyterlab>=4.0
ipykernel>=6.29

# === Utilities used in the notebook ===
pydrive2>=1.20.0
pillow>=10.3.0

# (Optional, helpful in practice)
matplotlib>=3.8
numpy>=1.26
